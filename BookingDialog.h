#pragma once
#include <QDialog>

// Placeholder for future detailed booking dialog
// Currently booking is handled inline in MarketScheduleScreen
class BookingDialog : public QDialog {
    Q_OBJECT
public:
    explicit BookingDialog(QWidget* parent = nullptr);
};
