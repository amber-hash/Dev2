#pragma once
#include <QDialog>

// Placeholder for future detailed waitlist management dialog
class WaitlistDialog : public QDialog {
    Q_OBJECT
public:
    explicit WaitlistDialog(QWidget* parent = nullptr);
};
